name: Mobile splash dependency sync

on:
  push:
    branches:
      - mobile-foundation-refactor
    paths:
      - '.github/workflows/mobile-splash-sync.yml'

permissions:
  contents: write

jobs:
  sync:
    name: Install Expo splash screen
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout mobile branch
        uses: actions/checkout@v4
        with:
          ref: mobile-foundation-refactor

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22.13.1'
          cache: npm
          cache-dependency-path: mobile/package-lock.json

      - name: Install locked dependencies
        run: npm ci --prefix mobile

      - name: Install supported splash package
        working-directory: mobile
        run: npx expo install expo-splash-screen

      - name: Commit generated package files
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add mobile/package.json mobile/package-lock.json
          if git diff --cached --quiet; then
            echo "No dependency changes required."
            exit 0
          fi
          git commit -m "chore(mobile): add Expo splash screen plugin"
          git push origin HEAD:mobile-foundation-refactor
