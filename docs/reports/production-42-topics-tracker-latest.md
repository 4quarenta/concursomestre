# Tracker - 42 Observacoes de Producao

Data base: `2026-05-16`

## Resumo atual

- **Concluidos:** `42`
- **Parciais:** `0`
- **Pendentes:** `0`

## Status por observacao

| # | Status | Observacao (resumo) | Evidencia atual |
| --- | --- | --- | --- |
| 1 | Concluido | Reset abre home antes da tela de reset | Redirecionamento canonico no servidor via `src/proxy.ts` + aliases em `src/app/page.tsx` para links antigos de reset |
| 2 | Concluido | Link de ativacao nao abre fluxo de parabens esperado | Canonizacao de auth foi centralizada em `src/services/auth/canonicalAuthRedirect.ts`, com cobertura no `proxy`, na raiz (`src/app/page.tsx`) e em rotas alias server-side (`/activate`, `/activation`, `/verify-email`, `/confirm`), garantindo que links legados caiam direto em `/confirm-email`; testes adicionados em `canonicalAuthRedirect.test.ts` |
| 3 | Concluido | Tempo de estudos com media diaria | `src/app/dashboard/DashboardPage.tsx` |
| 4 | Concluido | Desempenho por materia sem falso "sem dados" | `src/app/performance-subjects/PerformanceSubjectsPage.tsx` |
| 5 | Concluido | Avaliar plataforma sem pagina propria, via modal | `src/app/profile/ProfilePage.tsx` |
| 6 | Concluido | Foto atualizada refletindo no layout global | `Layout` passou a priorizar `currentUser.photoUrl` e a sessao/auth normaliza `photo_url -> photoUrl` no bootstrap/refresh, garantindo reflexo consistente apos upload (`src/components/shared/layout/Layout.tsx`, `src/services/auth/session.ts`) |
| 7 | Concluido | Dashboard do aluno com foco em "hoje" por padrao | `src/app/dashboard/DashboardPage.tsx` |
| 8 | Concluido | Foco sem destrinchar subitens | `src/services/filters/index.ts` |
| 9 | Concluido | Badge "Inedita" centralizado | `src/app/questions/components/QuestionCard.tsx` |
| 10 | Concluido | Comentario/analise com melhor formatacao de texto | Renderizador matematico/comentarios agora normaliza HTML legado para Markdown antes da sanitizacao (`MathRichText`), evitando exibicao crua de `<ul><li>` e suportando negrito/italico/sublinhado de forma consistente |
| 11 | Concluido | Sair da tela cheia no simulado perto de finalizar | `src/app/simulation/page.tsx` |
| 12 | Concluido | Border-radius global mais contido | Padrao visual consolidado para `rounded-2xl` nos containers principais e overlays compartilhados. A constante global `PLATFORM_SURFACE_CARD_CLASS` saiu de `rounded-[2rem]` para `rounded-2xl`; foram normalizados tokens grandes (`rounded-3xl`, `rounded-[2rem]`, `rounded-[2.5rem]`) em `src/app` e `src/components/shared`, cobrindo dashboard, questoes, pratica, simulados, desempenho por materia, login/reset/confirmacao, checkout, planos, marketplace, ranking, perfil, paginas legais, landing/promo e modais compartilhados. Validação: `rg` sem ocorrencias desses tokens em `src/app`/`src/components/shared`, `npm run lint`, `npm run check:text-encoding` e `git diff --check` OK |
| 13 | Concluido | Foco de estudo no perfil baseado em dados da plataforma | Modal "Escolha seu foco" passou a consumir `systemSettings.taxonomies.careers` com normalizacao de label e carregamento de taxonomias via store (`useTaxonomyActions`), mantendo fallback seguro |
| 14 | Concluido | Acesso ao crawler do Gran no admin | Nova secao operacional `Crawler Gran` em Operacao com entrada oficial para `scripts/importers/questions/gran/index.php` (`src/app/admin/components/import/AdminGranCrawlerSection.tsx`) |
| 15 | Concluido | Telefone obrigatorio no cadastro e dados pessoais | `Auth.tsx`, `ProfilePage.tsx`, backend auth validators |
| 16 | Concluido | Confirmacao de excluir notificacao fora do padrao | Fluxo usa modal padrao (`useConfirm`) em limpar lixeira e excluir permanente em `src/app/notifications/page.tsx` |
| 17 | Concluido | Comentarios com deteccao automatica de spam | `modules/comments/services/CommentsService.php` |
| 18 | Concluido | Dashboard admin com "hoje" por padrao | `src/app/admin/components/dashboard/AdminDashboard.tsx` |
| 19 | Concluido | `admin/support/feedback` no padrao WordPress | `src/app/admin/components/support/AdminFeedback.tsx` |
| 20 | Concluido | Resolver denuncia direto da questao aberta | Tela de edicao de questao com denuncia vinculada agora permite resolver/ignorar inline, sem voltar para a fila de suporte (`src/app/admin/operation/questions/[questionId]/edit/page.tsx`) |
| 21 | Concluido | Texto de visibilidade e outros ajustes de escrita | Varredura visivel concluida no recorte critico de questoes, pratica, dashboard, suporte e admin de questoes. Foram corrigidos labels/CTAs/erros como "Questão", "Inédita", "Órgão", "Múltipla escolha", "histórico", "denúncia", "comentário" e mensagens de estado vazio em `src/app/question/QuestionPublicPage.tsx`, `src/app/question/layout.tsx`, `src/app/questions/components/QuestionCard.tsx`, `src/app/practice/page.tsx`, `src/app/dashboard/DashboardPage.tsx`, `src/app/support/page.tsx`, `src/app/admin/operation/questions/[questionId]/edit/page.tsx` e componentes admin de questões. Validação: `npx eslint` direcionado sem warnings e `npm run check:text-encoding` OK |
| 22 | Concluido | Add/edit admin muito lentos | Tuning aplicado nas rotas de edicao: editor de questao deixou de receber o banco inteiro em memoria (passa apenas questao ativa), editor de prova parou de fazer merge pesado com todas as questoes no mount, editor de Lei Comentada passou a reaproveitar taxonomias do store (com fallback), evita reload duplicado por chave/promise no mount e so consulta lote editorial ao abrir a aba de IA, editor de Landing deixou de consultar planos quando esta apenas na listagem, `getUserDetails/getAdminDetail/getAdminLawUpdates` ganharam deduplicacao in-flight com TTL curto, `getQuestionForAdminEdit` ganhou coalescencia por 15s no service, `operation/questions/[questionId]/edit` passou a deduplicar carga por `questionId` em remounts, e `operation/exams/[examId]/edit` passou a preservar draft hidratado evitando rehidratacao desnecessaria durante edicao. Nesta etapa, o editor de Lei Comentada tambem recebeu limpeza estrutural (sem duplicidade Resumo/Ementa, novo campo de Preambulo, vinculo Materia -> Lei -> Titulo -> Capitulo enxuto, labels de taxonomia normalizadas para evitar exibir IDs crus como 503/504 e fallback hierarquico consistente). Lei Comentada publica tambem recebeu carga de secoes em modo leve (`outline=1`) com timeout dedicado e cache separado para evitar "Carregando secoes da lei..." infinito. Evidencia local final: editores standalone usam endpoints de detalhe dedicados, nao fazem bootstrap do banco inteiro, `npm run check:hard-refresh-budget` passou e `npm run check:production-local` passou. Homologacao de massa real/staging permanece como validacao macro de release, nao como pendencia deste topico. |
| 23 | Concluido | Upgrade manual admin sem cobranca | `manual_admin` virou provider nao cobravel oficial. Upgrade manual cancela assinatura Stripe remota antes de conceder beneficio gratuito; dias acrescidos pelo admin tambem viram concessao `manual_admin`, limpam `provider_subscription_id`, projeções de renovacao e `auto_renew`; duracoes curtas (`day`/`week`) foram preservadas para plano teste de 2 dias. Validacao local: `php -l` em `payment_provider.php`, `AdminUserActionsService.php`, `AdminUserActionsRepository.php` e `AdminUserActionsWiringTest.php` OK. Homologacao Stripe real permanece na auditoria macro de billing/staging, nao como pendencia deste topico. |
| 24 | Concluido | Perfil detalhado com denuncias/feedback/sugestoes/avaliacoes | Backend exposto em `AdminUserDetailsService/Repository` e nova aba `Relacionamento` no modal (`UserProfileAdminModal.tsx`) |
| 25 | Concluido | Tabela vendedores estilo usuarios + gestao completa | `AdminFinance` recebeu grade WP-like com busca/filtro, selecao em massa, acao em lote (ativar/suspender/banir), acoes por linha (ver/editar/vendas/produtos), modal com pagamentos/documentos e moderacao rapida de materiais (aprovar/rejeitar). Nesta etapa, o modal passou a normalizar `authorId`, `author_id`, `sellerId` e `seller_id`, evitando vendedor com contador correto e lista vazia em massa real; a suite `adminMarketplaceMetrics.test.ts` cobre vendedores, materiais publicados, saldo disponivel/retido e badges do dashboard, e entrou no `check:production-local`. Validacao local: ESLint direcionado e Vitest OK; homologacao com marketplace real fica como smoke macro de release. |
| 26 | Concluido | Badge de materiais aguardando aprovacao no menu | `AdminStats` + `useAdminPageController` + `AdminNavigationSidebar` |
| 27 | Concluido | Cobranca em risco sem detalhamento de falha/usuario | Painel financeiro agora lista usuario, email, sinal e ultimo evento em `src/app/admin/components/finance/AdminFinanceAnalyticsPanel.tsx` |
| 28 | Concluido | Cenarios Stripe "partial/not supported" | Suite E2E local com Stripe em modo teste fechou `GO` (`7 OK`) e o checklist agregado fechou `GO` (`22 OK`). Coberto: renovacao por Test Clock, auto-renew off/on, upgrade com credito proporcional local, refund concorrente, webhook duplicado, fora de ordem e atrasado com reconciliacao. O plano teste curto respeita duracao/preco de 2 dias, reembolso concluido cancela assinatura imediatamente, e renovacao usa preco vigente do plano sem reaplicar cupom de checkout. A verificacao periodica de cartao vencido/proximo do vencimento foi promovida para `scripts/tasks/check_subscription_card_expiry.php`, com guarda CLI e cron documentado. A reconciliacao Stripe tambem ganhou tarefa CLI dedicada (`scripts/tasks/reconcile_stripe_subscriptions.php`) para rodar sem usuario logado; lembrete de 5 dias passou a ser exclusivo de ciclos maiores que 5 dias e o template `subscription_renewal_tomorrow` cobre o aviso "renova amanha". Nesta etapa, a reconciliacao passou a gravar heartbeat privado (`subscription_cron_health.json`) e o webhook Stripe passou a gravar heartbeat privado (`stripe_webhook_health.json`); endpoints Mercado Pago descontinuados respondem `410` direto sem abrir MySQL; renovacao paga agora gera notificacao in-app especifica de `Assinatura renovada` e a aba de assinatura bloqueia snapshot vencido enquanto sincroniza a Stripe. O admin exibe ambos os heartbeats em `Painel > Saude do billing` e o preflight reprova producao com heartbeat ausente/antigo, reduzindo risco de falha silenciosa de cron ou webhook. Falta apenas homologacao operacional do dominio/VPS, tratada na auditoria macro, nao como pendencia funcional deste topico. |
| 29 | Concluido | `CRON_SECRET` nao configurado | Localmente fechado por preflight e runbook: `CRON_SECRET` forte e obrigatorio, `APP_ENV=production`, `APP_URL` HTTPS/publico, CORS HTTPS sem localhost/wildcard, usuario DB nao-root, `DB_PERSISTENT=false`, timeout DB entre 1-10s, heartbeat de cron/webhook e `.env.production.example` para VPS. Validacao: `ProductionPreflightWiringTest.php`, `ProductionPreflightBehaviorTest.php` e `check:production-local` OK. A troca dos valores reais na VPS permanece checklist de deploy, nao pendencia funcional do codigo. |
| 30 | Concluido | Limpeza de "lixo" em configuracoes | Removido bloco duplicado de Homepage em Configuracoes Gerais e componente legado eliminado (`src/app/admin/components/settings/AdminSettings.tsx`, `src/app/admin/components/settings/AdminLandingContentSection.tsx`). Varredura atual do admin de settings nao encontrou `Homepage`, `homePage`, `landingContent` ou `AdminLandingContent`; permanece apenas a configuracao estrutural de landing usada pela homepage publica |
| 31 | Concluido | Seguranca admin com IP suspeito/banimento | Novo endpoint `admin/security_ips.php` com lista de IPs suspeitos (sinais e score), bloqueio/desbloqueio manual no painel (`AdminSettings`), tabela de ban (`security_ip_bans`) e enforcement real nas rotas de auth/middleware (`IpBanGuard`) |
| 32 | Concluido | Google login pedindo dados pessoais se conta nao existir | Fluxo fechado localmente: o primeiro login Google envia `createIfMissing: false`, conta inexistente cai em formulário dedicado de Dados Pessoais, sem misturar com cadastro por senha, e só depois envia `createIfMissing: true` com nome e telefone normalizado. Backend rejeita criação social sem telefone/nome, reaproveita o guard para Facebook/Apple e valida DDD com 10/11 digitos. Validacao: `SocialAuthProfileCompletionWiringTest.php`, `GoogleAuthWiringTest.php` e ESLint direcionado em `Auth.tsx` OK. Homologacao OAuth real fica na auditoria macro de staging, nao como pendencia funcional deste topico. |
| 33 | Concluido | Remover cupom re-adiciona | `src/state/app-config/systemSettings.ts` normaliza cupons, trata `null` como lista vazia e completa respostas parciais do backend sobre o estado salvo; `src/state/app-config/__tests__/systemSettings.test.ts` trava default vazio, remocao e resposta parcial |
| 34 | Concluido | Login/cadastro Facebook | Fluxo fechado localmente: frontend carrega SDK oficial somente quando configurado, primeiro passo usa `createIfMissing=false`, conta inexistente cai no mesmo formulário dedicado de Dados Pessoais, backend valida token por `debug_token`, rejeita `app_id` divergente, exige telefone antes de criar usuario e o admin valida App ID numerico. `.env.example` e `.env.production.example` documentam as chaves. Validacao: `SocialAuthProvidersWiringTest.php`, `SocialAuthProfileCompletionWiringTest.php` e CSP test OK. Homologacao Meta real fica na auditoria macro de staging, nao como pendencia funcional deste topico. |
| 35 | Concluido | Login Apple | Fluxo fechado localmente: frontend carrega AppleID SDK somente quando configurado, primeiro passo usa `createIfMissing=false`, conta inexistente cai no formulário dedicado de Dados Pessoais, backend valida assinatura com chaves Apple, `aud`, `iss`, expiracao e e-mail verificado, e o admin valida Client ID/redirect URI. `.env.example` e `.env.production.example` documentam as chaves. Validacao: `SocialAuthProvidersWiringTest.php`, `SocialAuthProfileCompletionWiringTest.php` e CSP test OK. Homologacao Apple real fica na auditoria macro de staging, nao como pendencia funcional deste topico. |
| 36 | Concluido | Metodos de pagamento do painel nao aparecem no checkout | Checkout agora consome metodos ativos do painel em tempo real, permite selecao dinamica (cartao/pix/boleto/wallets) e envia `payment_method_id` ao backend; admin ganhou controle de `checkoutSupported` e `recurringSupported` por metodo (`CheckoutPage`, `CheckoutPaymentStage`, `StripePaymentMethodsSettings`, `SubscriptionsValidator`, `SubscriptionsService`) |
| 37 | Concluido | Reportar comentario com motivo (igual reportar questao) | `src/components/shared/feedback/CommentsSection.tsx` |
| 38 | Concluido | Muitos erros/violations no console | Fluxos de runtime passaram para `clientLog`, preservando diagnostico no DebugLogger e evitando `console.error/warn/log/debug` direto salvo modo verbose. A varredura atual de `src` encontra apenas 2 ocorrencias intencionais dentro do proprio `src/services/monitoring/clientLog.ts`. Recorte validado com ESLint direcionado sem warnings, `typecheck`, `check:text-encoding` e `check:production-local` |
| 39 | Concluido | Limpeza de codigo morto + comentarios | Higiene local concluida no recorte de producao. Runtime sem `quick-login`, `debugger`, `DataProvider/useData`, `alert/confirm/eval` nativos e com apenas os `dangerouslySetInnerHTML` conhecidos por sanitizacao/JSON-LD. `DebugBanner` ficou restrito a desenvolvimento e ativacao explicita. `tsconfig.tsbuildinfo` saiu novamente da raiz/versionamento, o cache incremental foi redirecionado para `.next/tsconfig.tsbuildinfo`, e a nova trava `npm run check:generated-artifacts` entrou no `check:production-local` para impedir retorno de artefatos gerados na raiz. Relatorio de remocao registra que nao ha candidato seguro imediato pendente; proximas limpezas ficam condicionadas a validacao por dominio. Validacao: `npm run typecheck`, `npm run check:generated-artifacts`, `npm run check:text-encoding`, `npm run check:production-local` e `git diff --check` OK. |
| 40 | Concluido | Visualizador de logs com visao completa | `src/app/admin/components/settings/LogViewer.tsx` |
| 41 | Concluido | Lista objetiva de arquivos removiveis | Relatorio objetivo mantido em `docs/reports/production-removal-candidates-latest.md`; limpeza real aplicada em artefatos locais (`.codex-dev*.log`, `.tmp-dev*.log`, `.tmp-next-start*.log`, `.tmp-dev3000-webpack-*.log`, `tmp-next-start-3102.log`), baseline de hard refresh movido para `docs/reports/artifacts/hard-refresh-baseline-latest.json`, componente legado removido, `tsconfig.tsbuildinfo` removido do versionamento, SDK legado `mercadopago/dx-php` arquivado fora de `htdocs` e bloqueado no preflight por `PAYMENT_LEGACY_MERCADOPAGO_SDK_REMOVED`, `backend.zip` movido para `C:/xampp/private-backups/questao-pro-backend/backend.zip` fora de `htdocs`, `storage/backups/legacy-code` movido para arquivo privado e bloqueado por `LEGACY_CODE_BACKUPS_OUTSIDE_PUBLIC_ROOT`, scripts de desenvolvimento/debug/setup/maintenance/seed arquivados e migracoes PHP legadas de `scripts/migrations` movidas para `C:/xampp/private-backups/questao-pro-backend/dev-scripts-archive/2026-05-18`. Em `2026-05-22`, os dois logs webpack antes bloqueados foram removidos apos encerrar o processo local que segurava o lock; o dev server foi religado na porta `3000`. Nao ha candidato imediato pendente; limpezas futuras ficam condicionadas a validacao por dominio |
| 42 | Concluido | Modelos de e-mail editaveis no admin | Secao pronta local; catalogo recebeu `subscription_renewal_tomorrow` e `marketing_campaign_message`, cobrindo tambem campanhas automaticas. O envio real usa resolvedor unico de SMTP (`shared/utils/MailConfiguration.php`) com settings do painel + fallback `.env`; o preflight reprova producao sem SMTP, remetente valido ou templates essenciais ativos. O admin testa um modelo especifico pelo modal, enviando o rascunho atual para destinatario de teste (`test_email_template`) e validando SMTP + assunto + HTML/texto + placeholders antes de salvar/publicar. `MarketingAutomationService` deixou de montar e-mail direto e passou a usar `resolveSystemEmailTemplate`. `EmailTemplatesCatalogParityTest.php` trava paridade frontend/backend e impede que novo tipo de e-mail fique fora do painel. Validacao: `AdminSettingsWiringTest.php`, `EmailTemplatesCatalogParityTest.php`, `MarketingAutomationWiringTest.php`, preflight e typecheck OK. Homologacao SMTP real na VPS/dominio final fica no checklist macro de release, nao como pendencia funcional deste topico. |

## Auditoria de producao (macro)

- Veredito atual: **Nao pronto para go-live final; pronto para homologacao controlada em VPS/staging**.
- Os 42 topicos operacionais foram fechados localmente com testes e documentacao.
- Evidencia macro mais recente (`2026-05-22`): `npm run check:production-local -- --with-build` passou com build Next/Turbopack completo; suite PHP critica passou para preflight, auth social, modelos de e-mail, campanhas e assinaturas; `git diff --check` passou sem erro de whitespace.
- Proxima etapa preparada: `production_smoke.php` agora valida HTTPS em host publico, headers minimos de seguranca, endpoints publicos, rotas web e MySQL; sintaxe, wiring e smoke API/DB local passaram.
- Ainda falta evidencia real do ambiente final: dominio HTTPS, preflight na VPS, SMTP real, OAuth Google/Facebook/Apple real, webhooks Stripe publicos e smoke de fluxos criticos com dados de producao/sandbox.
