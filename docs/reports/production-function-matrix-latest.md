# Matriz Funcional de Producao

Data: `2026-05-01`

Legenda: `OK`, `Parcial`, `Risco`, `Bloqueado`.

| Area | Pagina/endpoint | Regra esperada | Estado atual | Falha/evidencia | Proxima acao |
| --- | --- | --- | --- | --- | --- |
| Homepage | `/` | Converter visitantes com copy objetiva, planos reais e depoimentos aprovados | Parcial | Build passa; planos vem do banco; depoimentos usam aprovados completos via `api/feedback/testimonials.php` e fallback mock quando nao houver nenhum; precisa prova visual final | Smoke visual desktop/mobile, plano real e depoimento aprovado |
| Login | `/auth` | Login/cadastro, Google, recuperacao e redirecionamento | Parcial | Frontend valida Client ID Google, `/auth` passa ESLint sem avisos, `AuthProvider.tsx` tambem passa ESLint e backend rejeita Client ID malformado ou token com `aud` divergente; precisa credencial real e smoke OAuth | Testar Google OAuth em staging |
| Layout global | Rotas com shell autenticado | Menu, perfil, modais de aviso e troca de rota | OK local parcial | Modal de verificacao e fechamento de menus nao disparam mais estado sincrono em efeitos; falta smoke browser | Navegar desktop/mobile, abrir menu perfil, trocar rota e fechar modal |
| Settings publicas | `api/settings.php` | Entregar somente configuracoes publicas, independente do bearer token enviado pelo navegador | OK local | Rota publica chama `show(null)`, nao usa `AuthMiddleware::optionalAuth()` e oculta `stripeKey` legado; teste wiring passou | Smoke anonimo e logado comum/admin na VPS, comparando ausencia de segredos |
| Settings admin | `api/admin/settings.php` | Exigir admin/staff para leitura e escrita de configuracoes sensiveis | OK local | GET/POST passam por `requireAdminSessionContext`; frontend usa endpoint admin somente para admin/staff; teste `adminService` passou | Smoke painel settings com admin real |
| Questao publica | `/question/[id]` | SEO, CTA para conta gratis, login modal para responder | Parcial | Build passa; carregamento client-side sem setState sincrono no efeito; precisa smoke anonimo/logado e schema/OG | Teste manual + Lighthouse |
| Pratica | `/practice` | Responder questoes, XP, stats, modo foco | Parcial | Backend answer passa; streaks/badges basicos implementados; filtros auxiliares estaveis, modo foco sem `setState` sincrono no efeito de carregar mais, rastreador de tempo sem render impuro e card de questao com ESLint warning zero; falta smoke real | Testar fluxo real com usuario gratis/elite |
| Questao anulada | `/question`, `/questions` e `/practice` | Nao permitir resposta, destacar visualmente | OK local | Flags centralizadas impedem falso positivo com `"0"`/`"false"` e mantem borda/bloqueio consistentes; `QuestionCard.tsx` passa ESLint sem avisos e nao usa relogio impuro no render; falta fixture real em staging | Criar fixture anulada e smoke |
| Admin questoes | `/admin/operation/questions/*` | CRUD, ineditas, filtros, contexto, aprovacao | Parcial | Build passa; editor sem avisos criticos de efeito/deps/render impuro no recorte; geracao IA e fluxo completo precisam QA manual | Testar criar/editar/publicar |
| Importador de questoes | `/admin/operation/import` | Extrair prova/gabarito, gerar comentarios e revisar antes de publicar | OK local parcial | `AdminImportSection.tsx` passa ESLint sem avisos e typecheck; preview tipado sem `any`; fluxo ainda precisa prova real com PDF/gabarito | Smoke importar PDF, gerar comentarios, editar questao e publicar |
| Contexto de questoes | Admin questoes | Tabela WP, modal, upload, vincular ids | Parcial | Implementado em UI anterior, sem teste automatizado | Teste funcional e upload |
| Admin database/navegacao | `/admin/operation/database/*` | Alternar tabelas/categorias no padrao WordPress e respeitar URL/hash | OK local parcial | Estado inicial resolve a aba da URL antes do render e sincronizacao posterior nao dispara render em cascata; falta smoke visual | Smoke clique em categorias, subtabs e deep link com hash |
| Admin taxonomias/filtros | `/admin/operation/database/filters` | CRUD de banca, orgao, foco, cargo, materia, topico e assunto com dependencia pai/filho | OK local parcial | Workflow local e helper de referencias do add/edit questao passam eslint sem avisos; `filters/index.ts` agora normaliza taxonomias com ids string/tipos crus explicitos e ficou sem `any` no ESLint direcionado; cargo/topico/assunto continuam exigindo pai | Smoke criar/editar/excluir cada tipo e validar seletores no add/edit questao |
| Comentarios | `comments` + `/admin/support/comments` | Criar pendente, admin aprova/spam/trash, notificar admin e autor | Parcial | Notificacao admin e autor corrigida; aprovacao dispara social; testes wiring passam | Teste de moderacao ponta a ponta |
| Denuncias | `reports` + `/admin/support/reports` | Registrar denuncia, deduplicar, notificar admin e avisar denunciante quando resolvida | Parcial | Notificacao admin corrigida; resolucao admin notifica o denunciante; wiring passa | Testar decisao admin, email e notificacao no painel real |
| Feedback/suporte | `feedback` + `/admin/support/feedback` | Conversa usuario/admin, badge correto, notificacoes e deep link para thread | Parcial | Notificacao admin corrigida; resposta admin notifica o usuario; clique no menu principal Suporte abre a primeira fila com pendencia; `/support?threadId=...` abre a conversa correta; `AdminFeedback.tsx` e controller admin passam ESLint sem avisos; precisa smoke visual com dados reais | Smoke de nova conversa, resposta, notificacao do usuario e clique no badge |
| Depoimentos da home | `api/feedback/testimonials.php`, `/profile`, `/` | Usuario envia avaliacao com nome publico/contexto; admin aprova antes de aparecer na home; sem aprovadas usa mocks | OK local parcial | Validator exige rating, nome publico, contexto e texto; admin publica ao marcar `resolved`; endpoint publico lista apenas completos com `home_published_at`; testes frontend/backend passam | Smoke enviar avaliacao, aprovar em suporte, ver home e reverter status |
| Materiais marketplace | `materials` | Upload, moderacao, compra, bloqueio, notificacao e recompensa | OK local parcial | Notificacao admin para material pendente e notificacao de autor aprovar/rejeitar/remover cobertas; aprovacao gera XP/reputacao/badge idempotente; admin de materiais passa ESLint sem avisos; falta smoke de arquivo protegido | Testar upload real e arquivo protegido |
| Marketplace compra | `payments`, `transactions`, `/marketplace` | Compra material, acesso protegido, notificacao, XP/reputacao e reembolso | OK local parcial | Compra direta preserva `material_id` textual; Stripe verify-payment e compra gratuita notificam comprador/vendedor/admin e geram XP/reputacao/badges por ledger idempotente; provider frontend sem avisos criticos de efeito/deps no recorte; `transactionsService.ts` e `marketplaceService.ts` passaram no ESLint direcionado e a pagina marketplace segue sem avisos, sem mutar `currentUser` apos checkout e sem imagens diretas; falta sandbox real | Teste gateway, notificacoes, XP/reputacao e download |
| Dashboard de parceiro | `/partner-dashboard` | Parceiro acompanha vendas, materiais, notificacoes, dados bancarios e avaliacoes sem render instavel | OK local parcial | Pagina passa ESLint sem avisos e typecheck; saldo/grafico nao usam relogio impuro no render, notificacoes sairam do corpo do componente, previews usam `next/image` e salvar dados bancarios encerra loading corretamente | Smoke parceiro: publicar material, editar capa, responder avaliacao, salvar banco e abrir notificacoes |
| Assinaturas | `subscriptions/*` | Checkout, renovacao, cancelamento, past_due e webhook configuravel sem ambiguidade | Risco | Testes locais passam; chaves Stripe invalidas sao barradas; `past_due` notifica aluno/admin; `stripe_webhook.php` e alias `webhook_stripe.php` delegam para a mesma rota; `subscriptionsService.ts`, `paymentsService.ts` e camada HTTP passaram no ESLint direcionado e build/typecheck; ainda sem prova webhook publica | Matriz Stripe em staging |
| Configuracao Stripe | `config/stripe.php`, admin settings, preflight | Aceitar apenas chaves reais e coerentes antes de vender | OK local | Runtime valida `sk_*`, admin valida `pk_*`/`sk_*`/`whsec_*` e preflight reprova formatos invalidos ou modos test/live misturados; UI de metodos Stripe locais nao usa ID temporal em render e passa ESLint | Smoke de checkout/webhook com credenciais reais |
| Config publico de pagamentos | `api/payments/config.php` | Expor somente chave publica Stripe valida e nunca sinalizar checkout ativo com configuracao incompleta | OK local | `publishableKey` invalida vira string vazia e `stripeConfigured` exige publishable valida + secret valida | Smoke frontend checkout/marketplace com Stripe sandbox |
| Financeiro admin | `/admin/financeiro/*` | Ver transacoes, planos, cupons, automacao, reembolsos, analytics e evidencias Stripe | Parcial | `AdminFinance.tsx` e `AdminFinanceAnalyticsPanel.tsx` sem avisos criticos de setState/render impuro no recorte; execucao manual de automacao voltou a compilar com resposta tipada; tabelas e automacoes ainda precisam smoke com gateway real | Smoke transacao, refund, webhook, analytics, automacao e evidencia Stripe |
| Verificacao tardia de pagamento | `api/payments/verify-payment.php` | Confirmar PaymentIntent apenas para usuario autenticado dono da compra | OK local | Rota exige auth e service compara usuario autenticado com metadata/transacao antes de persistir acesso | Smoke compra material em staging |
| Stripe Connect vendedor | `api/payments/create-connect-account.php` | Criar onboarding apenas para usuario autenticado e com redirects seguros | OK local | Payload nao pode trocar `userId`; `returnUrl`/`refreshUrl` aceitam apenas origem da plataforma e HTTPS em producao | Smoke onboarding vendedor em staging |
| Cartao salvo | `users/cards` + Stripe | Nao duplicar cartao, atualizar PM recente | OK local | Teste `UsersCardsStripeMirrorUpsertTest.php` passou apos correcao | Provar com Stripe real |
| Reembolso | `transactions/refund` | Solicitar, aprovar, reconciliar gateway, notificar afetados e ajustar reputacao | OK local parcial | Pedido notifica admins e vendedor; conclusao notifica usuario, vendedor e admins; reembolso de material reduz reputacao do vendedor de forma idempotente; falta gateway real | Testar refund sandbox |
| Cron financeiro | `subscriptions/cron_stripe_reconciliation.php` | Rodar com `CRON_SECRET`, sem concorrencia excessiva e sem gravar artefato publico | Parcial | Secret e lock validados antes do MySQL; log interno vai para `storage/logs/subscriptions`; falta prova carga/agenda | Configurar cron unico, rotacao de log e smoke VPS |
| Cron legado MP | `scripts/tasks/sync_mercadopago_preapproval_plans.php` | Nao quebrar por arquivo ausente e orientar remocao | OK local | Script guard retorna provider removido | Remover cron MP da VPS |
| Notificacoes | `notifications/*`, `/notifications` | Listar apenas ativas, marcar, limpar, restaurar lixeira, enviar e alertar admins/usuarios em eventos criticos | OK local parcial | Schema/listagem corrigidos; feedback respondido, comentario moderado, curtida social e denuncia resolvida notificam usuarios; compra/reembolso de material, `past_due` Stripe e reembolso pendente notificam afetados; wiring passa; pagina `/notifications` passa ESLint sem avisos, sem relogio impuro no render e com evidencia em `next/image` | Testar UI, unread, restaurar/excluir, deep links e webhooks reais |
| Gamificacao | `questions/answer`, comentarios, denuncias, `rankings/*`, marketplace | XP, level, badges, streak, reputacao, ranking e notificacoes por evento | Parcial | XP/level/streak/badges basicos existem; marketplace, social e rankings possuem `user_gamification_events` para aprovacao/venda/reembolso, comentarios, curtidas, denuncias aceitas, participacao e resultado oficial; ainda falta motor geral/outbox | Expandir motor geral e testar webhooks/rankings reais |
| Rankings | `/ranking`, `rankings/*` | Listar, criar com auth, enviar gabarito sem confiar no `userId` cliente, moderar, notificar e recompensar eventos principais | Parcial | Create/join exigem usuario autenticado; join usa usuario da sessao; ranking pendente notifica admin; moderacao notifica e recompensa criador; participacao/gabarito oficial notificam e recompensam participantes; `rankingsService.ts` passou no ESLint direcionado e wiring passa; sem teste E2E | Smoke criar ranking com aluno, aprovar com admin, enviar gabarito, publicar gabarito oficial e abrir notificacoes/badges |
| Simulados | `/simulation`, `simulations/*` | Criar, executar, tempo so em andamento | OK local parcial | Wiring passa; `StudyTrackerProvider` nao usa tempo impuro no render, `/simulation` passa ESLint sem avisos, `simulationsService.ts` foi tipado para persistencia/listagem e o cronometro finaliza pelo tick sem cascata de estado no efeito; falta smoke real do timer e persistencia | Smoke manual iniciado/finalizado, restaurar historico e validar contagem de tempo |
| Cronograma | `/cronograma`, `api/study-schedule/*` | Modulo elite ativavel, plano de estudos persistido por usuario e sem indexacao publica | OK local | Backend persiste por usuario, bloqueia no servidor para Elite/admin, UI migra localStorage antigo e layout usa `noindex`; falta smoke com usuario Elite real | Testar criar/salvar/recarregar/apagar em staging |
| Raio-X | `/x-ray`, statistics | Insights de banca, graficos estaveis | Parcial | Graficos criticos do Raio-X/Bank Analysis usam wrapper que so monta Recharts com dimensao real; falta smoke visual em staging | Smoke responsivo |
| Graficos operacionais | Dashboard, perfil, parceiro e raio-x de banca | Nao emitir warning `width(-1)`/`height(-1)` quando cards/tabs ainda nao mediram | OK local | `ResponsiveContainer` direto removido dos graficos criticos e teste SSR do wrapper passou | Smoke browser desktop/mobile com console limpo |
| Lei comentada | `/lei-comentada` | Leis, comentarios, highlights, admin acervo | Parcial | Build passa; admin save/import exigem `requireAdminSessionContext($db)`; admin do acervo passa ESLint sem avisos; comentarios/moderacao precisam E2E | Testar artigo, comentario e admin acervo com usuario sem permissao |
| Leitor PDF/material | `PdfViewer` | Abrir PDF, comentarios, anotacoes e marcadores sem render instavel | OK local parcial | `PdfViewer.tsx` passa ESLint sem avisos e typecheck; comentarios foram tipados e efeitos de abrir/recarregar sidebar nao chamam estado sincrono | Smoke abrir PDF, comentar, responder, curtir, anotar e criar marcador |
| Analytics tracking | `api/analytics/track.php` | Registrar eventos sem confiar em identidade enviada pelo cliente e sem aceitar payload abusivo | OK local | Endpoint usa `analytics_track`, usuario autenticado vem da sessao, anonimo nao pode forjar `userId` e `metadata`/`externalHooks` tem limite | Observar volume real, taxa de 429 e eventos de campanha em staging |
| Performance materias | `/performance/subjects` | Insight elite e comparativo da comunidade | Parcial | Build passa; regra elite precisa smoke | Teste usuario gratis/elite |
| Perfil | `/profile` | Dados, cobranca, salvos, avaliar plataforma | Parcial | Modal de avaliacao exige nome publico/contexto/depoimento, envia foto quando houver e evita repetir dados administrativos no texto; questoes salvas/notas/foto/materiais sem efeitos criticos de render em cascata no recorte; UI precisa smoke | Testar abas, envio, validacoes e aprovacao admin |
| Campanhas | `/admin/marketing/campaigns`, `scripts/tasks/process_marketing_automations.php` | Notificacao manual, banners, segmentacao, automacoes por condicao e execucao idempotente | Parcial | UI salva regras/banners; `AdminMarketing.tsx` passa eslint sem avisos; executor CLI processa regras ativas com dry-run, lock, `marketing_automation_events`, notificacao/e-mail e condicoes oficiais; falta smoke com campanha ativa, SMTP real e usuarios elegiveis | Smoke campanha ativa: dry-run, envio real controlado, duplicidade, links e logs |
| Ofertas/countdown | Home/checkout/planos | Mostrar oferta limitada sem instabilidade de hidratacao | OK local parcial | Hook de countdown nao usa `Date.now()` no render e passa eslint sem avisos | Smoke visual de oferta ativa/expirada em desktop/mobile |
| Promocao publica | `/promo/[slug]` | Exibir somente a campanha ativa correta e proteger slugs invalidos de indexacao | OK local | Slug da rota precisa bater com `activePromotion.slug`; metadata SSR indexa somente slug ativo e usa `noindex` para inativo/invalido/modulo desligado | Smoke campanha real no dominio final |
| URLs de campanhas | Admin marketing, notificacoes e banners | CTAs devem ir para rotas seguras e existentes | OK local | Links configuraveis bloqueiam `javascript:`, `data:`, protocolo relativo e caminhos `/admin`/`/api`; fallback padrao agora e `/planos`, nao `/pricing` | Smoke admin criando banner/notificacao |
| Landing pages | `/admin/marketing/landing-pages`, `/l/[slug]` | Lista WP, add/edit, publicar HTML seguro e metadata publica correta | Parcial | Editor atual usa estrutura textual; textos/SEO removem markup e canonical inseguro; `/l/[slug]` tem metadata server-side para publicadas, `noindex` para rascunhos/inexistentes e sitemap inclui landings customizadas publicadas. HTML bruto futuro ainda precisa sandbox/politica | Testar publicacao, OG/canonical/sitemap no dominio e definir politica para codigo livre |
| Logs | `/admin/settings/logs` | Categorias, repeticoes, limpar/baixar e logs operacionais fora da pasta publica | Parcial | Endpoint agora retorna `analysis`; scanner CLI pronto; cron de assinaturas grava em `storage/logs/subscriptions`. Falta rotacao/alerta real na VPS | Configurar cron/logrotate e testar UI |
| Cache admin | `/admin/settings/cache` | Ver estatisticas, limpar cache, limpar expirados e ajustar TTL sem quebrar runtime | OK local parcial | Tela de cache passa ESLint sem avisos e typecheck; payload tipado e carregamento inicial agendado; falta executar contra cache real na VPS | Smoke estatisticas, clear, clean expired e TTL |
| Auditoria de logs | `scripts/tasks/production_log_audit.php` | Detectar criticos e repeticoes antes de incidente | OK local | Wiring passou; execucao local inspecionou settings/cron sem criticos | Rodar em producao com `LOG_AUDIT_FAIL_ON=critical` |
| Smoke operacional | `scripts/tasks/production_smoke.php` | Validar endpoints publicos, settings sem vazamento e conexao MySQL apos deploy/rollback | OK local | Smoke local passou com planos, questoes, settings sem chaves sensiveis e 3 conexoes MySQL; falta execucao na VPS | Rodar na VPS com dominio real |
| Hydration/transicoes | `PageTransition`, `NextRouteFrame` | Primeiro HTML da rota deve bater com o cliente e nao disparar hydration warning | OK local | `PageTransition` renderiza shell SSR estavel e teste cobre ausencia de estado animado no HTML inicial | Validar build real no browser desktop/mobile |
| Superficie publica da API | `api/`, `production_preflight.php` | Nao publicar logs, dumps SQL ou textos operacionais | OK local | `subscription_cron.log` removido de `api/`; serviço grava em `storage/logs/subscriptions`; preflight bloqueia novos artefatos publicos | Rodar preflight na VPS antes do go live |
| Admin dashboard/painel | `/admin/panel` | Contadores, alertas e saude de billing sem render instavel | OK local parcial | Abas sincronizam no frame seguinte, alertas/billing estao tipados sem `any` e ESLint direcionado passa; historico ainda precisa snapshot backend para contadores | Teste de agregacao por dataset e smoke visual |
| Admin marketing navegacao | `/admin/marketing/*` | Alternar landing pages, campanhas, temas e redes sociais sem estado derivado instavel | OK local parcial | `AdminMarketingSection` e `AdminSocialLinksManager` passam ESLint sem avisos; rascunho de redes sociais sincroniza sem `setState` sincrono no efeito | Smoke editar redes sociais, trocar abas e salvar |
| Reset administrativo de banco | `api/admin/reset_db.php` | Nunca ficar executavel por acidente em producao publica | OK local | `APP_ENV=production` forca modo producao, reset fica bloqueado por padrao e preflight falha se `ADMIN_DATABASE_RESET_ENABLED=true` | Manter desativado em producao e usar apenas em janela operacional com backup |
| Modulos ativaveis | `/admin/settings/modules` | Feature flags controlam acesso | Parcial | Build passa; precisa matriz por rota | Teste cada flag |
| SEO | `sitemap.xml`, `robots.txt`, public pages | Indexar publico, noindex privado e canonicals corretos | Parcial | `NEXT_PUBLIC_CANONICAL_URL` alimenta metadata/sitemap/robots; cronograma Elite corrigido para `noindex`; sitemap dinamico remove questoes draft/elite/internal, materiais pendentes/rejeitados e rankings pendentes/rejeitados; falta deploy/Search Console | Validar no dominio |
| Seguranca | Auth/CORS/uploads/admin | Cookies, refresh, CSRF, permissoes, XSS e configuracao segura de deploy | Risco | Auth tests passam; XSS em questoes/comentarios mitigado; uploads, rate limit, reset DB guard e preflight de CORS/APP_ENV prontos localmente; HTML bruto futuro ainda precisa politica dedicada | Auditoria de endpoints mutaveis e staging |
| Headers de seguranca | Next/API | CSP, clickjacking, nosniff, referrer, permissions policy e deploy em modo producao | OK local | Headers aplicados no Next e na API; smoke local confirmou API; preflight barra `APP_ENV` fora de production, `APP_URL` local e CORS sem HTTPS/wildcard | Validar no proxy HTTPS da VPS |
| Uploads | Materiais, perfil, contexto de questoes | Validar MIME real, tamanho, extensao segura e impedir execucao em `/uploads` | OK local | Helper central aplicado e `.htaccess` endurecido; wiring passou | Validar regra equivalente no servidor final |
| Rate limiting | Auth, comentarios, feedback, denuncias, uploads e analytics | Bloquear abuso por IP e assunto sensivel, retornar 429 com `Retry-After` | OK local | Perfis aplicados; proxy headers ignorados por padrao; preflight valida runtime gravavel; analytics tem perfil proprio 120/min | Calibrar limites em staging e monitorar falsos positivos |
| Banco | MySQL | Env, indices, integridade, schema, backup | OK local parcial | DB env corrigido; script de backup/verificador prontos; schema do marketplace normalizado para `user_id`, `material_id` textual, ratings/notas compativeis e ledger `user_gamification_events`; falta restore real e carga | Teste dump/restore, carga, indices e migration em staging |
| Backup operacional | `scripts/tasks/backup_mysql.php`, `verify_mysql_backup.php`, `restore_mysql_backup.php` | Dump diario com checksum, retencao, restore seguro e smoke pos-restore | Parcial | Local pronto: backup/verificador/restore dry-run, bloqueio do banco atual e bloqueio de dump destrutivo. Falta restore real em banco temporario da VPS | Rodar backup real, restore em staging/VPS e smoke apontado ao banco restaurado |
