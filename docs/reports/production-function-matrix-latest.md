# Matriz Funcional de Producao

Data: `2026-05-05`

Legenda: `OK`, `Parcial`, `Risco`, `Bloqueado`.

Atualizacao incremental (`2026-05-16`):
- Superficie publica backend local fechou no recorte auditado: `backend.zip` (`177 MB`) foi movido para fora de `htdocs`, `.htaccess` passou a bloquear backups/dumps/compactados, diretorios internos e `scripts/` fora do allowlist do crawler Gran, e o preflight ganhou `BACKEND_ROOT_ARTIFACTS_CLEAN`. Evidencias: `BackendRootCleanupWiringTest.php`, `ProductionPreflightWiringTest.php`, `ProductionPreflightBehaviorTest.php` e `ApiResidualSurfaceWiringTest.php` passaram; smoke HTTP confirmou `403` para debug/storage e `200` para `gran/index.php`.
- Billing/Stripe local fechou `GO` na suite E2E (`7 OK`) e no checklist agregado de renovacao (`22 OK`); renovacao e reconciliacao sao server-side, sem depender de usuario logado.
- Higiene de runtime avancou: logs diretos em servicos, stores e telas de runtime foram migrados para `clientLog` (`subscriptionsService`, `planService`, `notificationService`, auth/session, reputacao, analytics, checkout, practice, QuestionCard, moderacao, suporte admin, provas, taxonomias, rankings, logs admin, financeiro/admin, suporte, marketplace, perfil, PDF reader, cronograma, raio-x de banca, parceiro, simulados e leitor). Contagem atual em runtime: `2` ocorrencias restantes de `console.error/warn/log/debug`, ambas intencionais no wrapper `src/services/monitoring/clientLog.ts`.
- `planService`, `auth/session`, editor admin de provas, `src/types/global.ts`, servicos pequenos de perfil/comentarios/changelog/cronograma/denuncias/suporte/SEO/publicacao, flags de questoes/insights/simulado/Raio-X, Lei Comentada publica/reader, ranking, checkout Stripe/cartao salvo, perfil Stripe, marketplace payment modal, landing de planos, `DebugLogger`, tipos de Lei Comentada, `SuccessModal`, privacidade, comentarios, rotas publicas secundarias, layout global e mocks de testes de servicos tiveram `any` exposto removido ou warnings dirigidos eliminados no recorte auditado; a varredura runtime atual em `src` esta em `0` ocorrencias explicitas e typecheck/preflight local seguem verdes.
- Settings/cupons: `mergeSystemSettings()` normaliza cupons e `resolvePersistedSystemSettings()` preserva o estado salvo pelo admin quando o backend retorna payload parcial, evitando reaparecimento de cupom removido.
- Evidencia local: `npm run typecheck`, `npm run lint`, ESLint direcionado, `npm run check:text-encoding`, `npm run check:production-local -- --with-build` e `npm run build` passaram no recorte atual; snapshot agregado de lint esta em `0` erros e `0` warnings.

Atualizacao incremental (`2026-05-08`):
- `useAdminPageController` voltou ao limite arquitetural da suite critica sem quebrar o shell administrativo.
- Preflight consolidado reexecutado com sucesso (`npm run check:production-local` e `npm run check:production-local -- --with-build`).
- Canonizacao de auth/reset ficou centralizada em `canonicalAuthRedirect`, com cobertura em `proxy.ts`, rota raiz e aliases server-side (`/activate`, `/activation`, `/verify-email`, `/confirm`, `/recover`, `/forgot-password`, `/reset`), eliminando dependencia exclusiva do cliente para abrir `/confirm-email` e `/reset-password`.
- Novo teste dedicado `canonicalAuthRedirect.test.ts` cobre compatibilidade de tokens/modos legados.
- `AppConfigProvider` adiou o bootstrap de `admin/settings.php` em `/admin/panel/*` para uma janela posterior (`8s`), reduzindo a carga inicial do painel no hard refresh.
- `AdminCommentsModerationSection` migrou para `React Query` (chave estavel + cache curto + invalidacao por mutacao), eliminando o carregamento manual sujeito a rajadas.
- `check:hard-refresh-budget` passou a validar automaticamente duplicatas, falhas e budget de `xhr/fetch`/`load` para as rotas criticas medidas.
- Higiene de producao: `src` ficou sem `console.log`, `console.debug`, `debugger` ou `quick-login` residual; logs removidos de `QuestionCard`, `ReaderPage`, `PdfViewer` e `AdBanner`.
- Zustand/TanStack: `@tanstack/react-query` e `zustand` instalados, `QueryProvider` ativo e stores por dominio em `src/state`; nenhuma dependencia runtime de `DataProvider/useData` encontrada.
- Arquitetura: teste de guarda passou a bloquear reintroducao de `DataProvider`, debug logs e quick-login em runtime.
- Landings: payloads administrativos continuam textuais e agora bloqueiam canonical com protocolo relativo, protocolo executavel e caminhos sensiveis (`/admin`, `/api`, `/_next`, `/uploads`, `/storage`), com teste XSS dedicado.
- HTML rico: JSON-LD da questao publica usa serializacao segura e a arquitetura passa a bloquear `dangerouslySetInnerHTML` sem sanitizer/renderizador conhecido.
- Graficos: arquitetura bloqueia `ResponsiveContainer` direto do Recharts fora do wrapper `StableResponsiveContainer`, mantendo render SSR estavel e evitando container `-1`.
- SEO: sitemap bloqueia landings com slugs reservados ou prefixos sensiveis (`admin`, `api`, `auth`, etc.) e a suite `privateSeo` valida alinhamento entre robots e rotas privadas.
- Release local: `npm run check:production-local` consolida encoding, typecheck, hard-refresh budget e suites criticas de arquitetura/SEO/XSS/charts em uma unica trava repetivel.
- Baseline mais recente (`docs/reports/artifacts/hard-refresh-baseline-latest.json`) sem duplicatas/falhas:
  - `/dashboard` (`DCL=323ms`, `load=587ms`, `xhr/fetch=3`)
  - `/practice` (`DCL=285ms`, `load=512ms`, `xhr/fetch=4`)
  - `/admin/panel/dashboard` (`DCL=348ms`, `load=689ms`, `xhr/fetch=3`)
  - `/admin/support/comments` (`DCL=338ms`, `load=816ms`, `xhr/fetch=4`, `comments_moderation` 1x)
- `/practice` mudou o carregamento de notas para sob demanda (clique em `Anotar`), removendo `users/notes.php` do primeiro paint sem quebrar o fluxo de resposta/comentario.
- `/practice` passou a adiar o fetch de `users/answers.php` para pos-primeiro-paint (`3.2s`) e mantem carga imediata apenas com filtro `Excluir Respondidas`.
- `/dashboard` passou a adiar a leitura de `users/answers.php` para pos-primeiro-paint (`4s` + idle), reduzindo mais carga no hard refresh inicial.
- Baseline atualizado (`docs/reports/artifacts/hard-refresh-baseline-latest.json`) sem duplicatas/falhas:
  - `/dashboard` (`DCL=145ms`, `load=327ms`, `xhr/fetch=3`)
  - `/practice` (`DCL=149ms`, `load=293ms`, `xhr/fetch=4`)
  - `/admin/panel/dashboard` (`DCL=176ms`, `load=416ms`, `xhr/fetch=4`)
- `shared/auth/AuthLogger.php` recebeu deduplicacao de eventos de auth ruidosos em janela curta, com bypass por `AUTH_LOG_VERBOSE_EVENTS=true`.

Atualizacao incremental (`2026-05-07`):
- `hard-refresh-baseline.mjs` agora exporta `failedRequests` e `xhrFetchSummary` por rota para auditoria objetiva de carga inicial.
- `panel/dashboard` deixou de chamar `reportsList` no primeiro paint; o contador de denuncias abertas agora usa `dashboardAnalytics.counts.reports_count` e o preload de denuncias fica para `support/reports` e `panel/alerts`.
- `useAdminPageController` passou a iniciar no tab/secao da rota atual e deixou de montar `panel/dashboard` transitorio ao abrir outras paginas do admin.
- `refresh.php` passou a devolver usuario durante bootstrap (quando solicitado), e o cliente deixou de fazer `auth/me.php` imediatamente apos hard refresh logado.
- `NotificationsProvider` passou a adiar o primeiro fetch de notificacoes para janela pos-primeiro-paint, preservando polling e refresh por foco.
- `AdminDashboard` passou a adiar o carregamento do card "Feedback recente", removendo `admin/feedback.php` do primeiro paint no `panel/dashboard`.
- Fluxos de importacao/edicao de questoes e a pagina `/practice` tiveram limpeza de mojibake; `check:text-encoding` passou novamente.
- Baseline local reexecutado (`docs/reports/artifacts/hard-refresh-baseline-latest.json`) com `0` duplicatas e `0` falhas nas rotas:
  - `/dashboard` (`DCL=291ms`, `load=507ms`, `xhr/fetch=6`)
  - `/practice` (`DCL=296ms`, `load=497ms`, `xhr/fetch=10`)
  - `/admin/panel/dashboard` (`DCL=346ms`, `load=584ms`, `xhr/fetch=7`)
  - `/admin/support/comments` (`DCL=297ms`, `load=556ms`, `xhr/fetch=6`)
- Rodada mais recente apos ajuste de sincronizacao de estatisticas por rota:
  - `/dashboard` (`DCL=230ms`, `load=455ms`, `xhr/fetch=6`)
  - `/practice` (`DCL=247ms`, `load=456ms`, `xhr/fetch=8`)
  - `/admin/panel/dashboard` (`DCL=317ms`, `load=645ms`, `xhr/fetch=7`)
  - `/admin/support/comments` (`DCL=303ms`, `load=627ms`, `xhr/fetch=6`)
- Rodada apos otimizar bootstrap de autenticacao:
  - `/dashboard` (`DCL=144ms`, `load=310ms`, `xhr/fetch=5`)
  - `/practice` (`DCL=140ms`, `load=283ms`, `xhr/fetch=7`)
  - `/admin/panel/dashboard` (`DCL=176ms`, `load=399ms`, `xhr/fetch=6`)
  - `/admin/support/comments` (`DCL=177ms`, `load=374ms`, `xhr/fetch=5`)
- Rodada apos defer do primeiro fetch de notificacoes:
  - `/dashboard` (`DCL=135ms`, `load=296ms`, `xhr/fetch=4`)
  - `/practice` (`DCL=174ms`, `load=302ms`, `xhr/fetch=6`)
  - `/admin/panel/dashboard` (`DCL=175ms`, `load=395ms`, `xhr/fetch=5`)
  - `/admin/support/comments` (`DCL=167ms`, `load=371ms`, `xhr/fetch=4`)
- Validacao consolidada da rodada (mesma execucao em 4 rotas):
  - `/dashboard` (`DCL=127ms`, `load=297ms`, `xhr/fetch=4`)
  - `/practice` (`DCL=126ms`, `load=272ms`, `xhr/fetch=6`)
  - `/admin/panel/dashboard` (`DCL=184ms`, `load=436ms`, `xhr/fetch=4`)
  - `/admin/support/comments` (`DCL=232ms`, `load=465ms`, `xhr/fetch=4`)

Atualizacao incremental (`2026-05-05`):
- `AdminCommentsModerationSection` agora bloqueia reload redundante para mesma chave de filtro/pagina e so forca recarga em acao explicita.
- `AdminSupportSection` deixou de disparar prefetch pendente por mudanca de callback do pai.
- Link legado `#/auth` na caixa de comentarios foi normalizado para `/auth`.
- `marketplaceService` ganhou coalescencia curta para `materialsList` e `transactions/list`.
- `NotificationsProvider` passou a ignorar refresh de foco/visibilidade quando a aba estiver em segundo plano.
- `AppConfigProvider` passou a segurar settings publicas enquanto a sessao autenticada ainda esta indefinida (token presente sem usuario), reduzindo duplicate fetch no bootstrap.
- `useQuestionBankActions` e `useUserProgressActions` passaram a bloquear fetch durante `authIsLoading`.
- `StudyTrackerBridge` deduplica `statistics/user` por usuario + escopo (dashboard/widget).
- `notificationService.sendNotification` nao envia quando token esta ausente/vencido (best-effort silencioso para reduzir 401 ruidoso).
- Backend de reCAPTCHA aceita o par oficial de teste apenas em localhost para evitar bloqueio indevido em dev.
- `AppConfigProvider` agora so usa `admin settings` em rotas `/admin`, mantendo o shell de estudo com payload publico mesmo para admins.
- `StudyTrackerBridge` agora aplica TTL de sincronizacao de estatisticas persistidas por usuario, evitando refetch em trocas curtas de rota.
- `MarketplaceProvider` passou a reger recarga de transacoes por identidade estavel (`userId` + privilegio), evitando refetch por mudancas de perfil sem impacto no financeiro.
- `MarketplaceProvider` deixou de preloadar materiais/transacoes em todo `/admin`, limitando bootstrap pesado as rotas `/admin/marketplace` e `/admin/finance`.
- `useAdminPageController` migrou para preload condicional de datasets por aba/secao (users/taxonomias/rankings), reduzindo carga no hard refresh do painel.
- `useAdminPageController` consolidou contadores de suporte (`feedback`, `pending_comments`, `refund_requests`) em uma unica chamada `adminService.getStats`, removendo requests redundantes no bootstrap.
- `useAdminPageController` passou a carregar `reports` apenas em dominios que realmente usam esse dataset no primeiro paint (`panel`/`support`).
- `adminService.getReports` ganhou coalescencia curta para remounts no shell admin.
- Confirmacoes destrutivas no admin (remover lei comentada, limpar logs e remover prova) migraram de `window.confirm` para `useConfirm` com modal padrao.
- A suite `adminArchitecture.test.ts` foi realinhada ao roteamento atual em Next (`NextRouteFrame`/`NextAppProviders`), mantendo a regra de bloquear `window.confirm` no admin.

| Area | Pagina/endpoint | Regra esperada | Estado atual | Falha/evidencia | Proxima acao |
| --- | --- | --- | --- | --- | --- |
| Homepage | `/` | Converter visitantes com copy objetiva, planos reais e depoimentos aprovados | Parcial | Build passa; planos vem do banco; depoimentos usam aprovados completos via `api/feedback/testimonials.php` e fallback mock quando nao houver nenhum; ornamentos sazonais deixaram de usar `Math.random()` no render SSR. Leituras quentes de home (`plans`, `testimonials`, `settings`) agora usam coalescencia curta e o bootstrap global deixou de puxar questoes/materials/transacoes fora de contexto, reduzindo a rajada de hard refresh. Ainda precisa prova visual/fetch final no browser | Smoke visual desktop/mobile, plano real, depoimento aprovado e network em hard refresh |
| Dashboard | `/dashboard` | Abrir direto na rota logada, sem landing intermediaria, com bootstrap enxuto e dados principais carregados sob demanda | OK local parcial | O shell autenticado foi separado da landing, a restauracao forcada de rota via `sessionStorage` saiu do `NextRouteFrame` e o bootstrap deixou de puxar `questionsList` no hard refresh. `statistics/user` ganhou coalescencia, `motivacoes-diarias.md` ganhou cache por sessao e o dashboard passou a usar `subjectBreakdown` de `statistics/user` em vez do banco inteiro de questoes. Settings/planos tambem deixaram de gerar URLs unicas por timestamp, reduzindo duplicidade visual no Network. Respostas e motivacao diaria agora entram em baixa prioridade, comentarios/notas ficaram fora do primeiro paint e o primeiro fetch de notificacoes foi adiado para pos-primeiro-paint. Ainda falta medir o padrao final de requests no browser do build/staging para confirmar a queda de carga e a fluidez percebida | Smoke de Network e UX em `/dashboard` logado, medindo refresh e transicao |
| Login | `/auth` | Login/cadastro, Google/Facebook/Apple, recuperacao e redirecionamento | OK local parcial | Frontend valida Client ID Google, `/auth` passa ESLint sem avisos, `AuthProvider.tsx` tambem passa ESLint e a tela de auth agora usa `authFlowService` + `readApiErrorMessage`, diferenciando falha da API e falha ao concluir a sessao local em vez de exibir "Erro de conexao com o servidor." genericamente. `interceptors.ts` tambem ganhou pre-refresh antes de enviar request com access token vencido, reduzindo `access_token_rejected` por `token_expired`. Login/cadastro/reset e auth do checkout migraram para reCAPTCHA v3 invisivel com validacao backend por `action`/`score`; quando a integracao estiver incompleta, a API publica agora devolve `recaptchaEnabled=false` e `recaptchaSiteKey=''`, evitando 422 por captcha semi-configurado. Nesta rodada, Facebook e Apple foram adicionados no frontend/backend + admin settings + CSP. O servidor dev voltou a servir CSP compativel com Google/reCAPTCHA/Meta/Apple apos restart. Ainda precisa credencial real e smoke OAuth/login completo | Testar login real, cadastro, reset e OAuth Google/Facebook/Apple em staging |
| Layout global | Rotas com shell autenticado | Menu, perfil, modais de aviso e troca de rota | OK local parcial | Modal de verificacao e fechamento de menus nao disparam mais estado sincrono em efeitos; `AdminNavigationSidebar.tsx`, `AdminTopBar.tsx`, `NotificationDropdown.tsx`, `AdminStandaloneShell.tsx`, `AdminShellLayout.tsx` e `components/shared/layout/Layout.tsx` passaram no ESLint direcionado, com contrato tipado de notificacoes e shell principal sem dropdown criado inline durante render; falta smoke browser | Navegar desktop/mobile, abrir menu perfil, trocar rota e fechar modal |
| Settings publicas | `api/settings.php` | Entregar somente configuracoes publicas, independente do bearer token enviado pelo navegador | OK local | Rota publica chama `show(null)`, nao usa `AuthMiddleware::optionalAuth()` e oculta `stripeKey` legado; CORS ramifica producao explicitamente e nao aceita origem local quando `APP_ENV=production`; teste wiring passou | Smoke anonimo e logado comum/admin na VPS, comparando ausencia de segredos |
| Settings admin | `api/admin/settings.php` | Exigir admin/staff para leitura e escrita de configuracoes sensiveis | OK local parcial | GET/POST passam por `requireAdminSessionContext`; frontend usa endpoint admin somente para admin/staff; `systemSettings.ts` normaliza cupons e preserva respostas parciais sobre o estado salvo; `AdminSettings.tsx` e `AdminSeoSettingsSection.tsx` passaram no ESLint direcionado sem `set-state-in-effect`, sem helpers mortos e com preview/cobertura SEO tipados; falta smoke completo do painel com 2FA/SMTP/integracoes | Smoke painel settings com admin real |
| Questao publica | `/question/[id]` | SEO, CTA para conta gratis, login modal para responder | OK local parcial | Build passa; carregamento client-side sem setState sincrono no efeito; JSON-LD usa serializacao segura antes do `dangerouslySetInnerHTML`; pagina passa ESLint direcionado sem avisos. Precisa smoke anonimo/logado e schema/OG no dominio | Teste manual + Lighthouse |
| Pratica | `/practice` | Responder questoes, XP, stats, modo foco | OK local parcial | Backend answer passa; streaks/badges basicos implementados; filtros auxiliares estaveis, modo foco sem `setState` sincrono no efeito de carregar mais, rastreador de tempo sem render impuro, taxonomias/filtros locais agora estao tipados sem `any` explicito e o card de questao segue com ESLint warning zero. O carregamento/paginacao de questoes migrou para `question-bank` dedicado (sem provider legado), incluindo bootstrap da primeira pagina quando a rota entra sem dados em memoria, evitando tela vazia. O `QuestionCard` tambem deixou de resetar selecao/feedback quando so as estatisticas da questao sao atualizadas apos responder e substituiu `alert()` por toast nos fluxos de denuncia/compartilhamento. Notas e respostas do progresso agora usam carga lazy no bootstrap (respostas imediatas apenas quando `Excluir Respondidas` estiver ativo), reduzindo carga de hard refresh sem perder o fluxo principal. Falta smoke real com base volumosa e conta gratis/elite | Testar fluxo real com usuario gratis/elite e scroll ate paginas antigas |
| Questao anulada | `/question`, `/questions` e `/practice` | Nao permitir resposta, destacar visualmente | OK local | Flags centralizadas impedem falso positivo com `"0"`/`"false"` e mantem borda/bloqueio consistentes; `QuestionCard.tsx` passa ESLint sem avisos e nao usa relogio impuro no render; falta fixture real em staging | Criar fixture anulada e smoke |
| Admin questoes | `/admin/operation/questions/*` | CRUD, ineditas, filtros, contexto, aprovacao | OK local parcial | Build passa; `ManualQuestionModal.tsx`, `AdminQuestionEditorPage.tsx`, `questionEditorShared.ts`, `useAdminQuestionsWorkflow.ts`, `useAdminManualQuestionEditor.ts`, `useAdminQuestionWorkbench.ts`, `useManualQuestionWorkflow.ts` e `AdminQuestionsSection.tsx` passaram no ESLint direcionado. A listagem principal saiu de `any` explicito no recorte auditado, o payload de geracao IA/taxonomias ganhou guardas reais e o link publico da questao so e montado quando existe ID numerico valido. Geracao IA e fluxo completo ainda precisam QA manual ponta a ponta | Testar criar/editar/publicar |
| Importador de questoes | `/admin/operation/import` | Extrair prova/gabarito, gerar comentarios e revisar antes de publicar | OK local parcial | `AdminImportSection.tsx`, `useAdminImportWorkflow.ts` e `useAdminImportSettingsBridge.ts` passam no ESLint direcionado e typecheck; o workflow ficou com tipos explicitos para PDF/IA/publicacao, removeu prop morta de settings e ganhou tratamento de erro mais previsivel. Ainda precisa prova real com PDF/gabarito | Smoke importar PDF, gerar comentarios, editar questao e publicar |
| Banco de provas | `/admin/operation/exams/[examId]/edit` e subaba de provas | CRUD global de provas, vinculo com questoes e busca editorial no padrao WordPress | OK local parcial | `AdminExamBankSection.tsx`, `AdminExamEditorPage.tsx`, `examBankUtils.ts` e `useAdminExamBankWorkflow.ts` passaram no ESLint direcionado; normalizacao das provas ficou tipada e a sincronizacao das questoes vinculadas saiu de casts frouxos. Falta smoke completo de criar/editar/remover prova com questoes reais | Smoke criar prova, editar taxonomias, remover e validar questoes vinculadas |
| Contexto de questoes | Admin questoes | Tabela WP, modal, upload, vincular ids | Parcial | Implementado em UI anterior, sem teste automatizado | Teste funcional e upload |
| Admin database/navegacao | `/admin/operation/database/*` | Alternar tabelas/categorias no padrao WordPress e respeitar URL/hash | OK local parcial | Estado inicial resolve a aba da URL antes do render e sincronizacao posterior nao dispara render em cascata; `useAdminDatabaseDatasets`, `useAdminTableSorting`, `SmartTagSelector`, `TaxonomyModal`, `useAdminDatabaseManagerController`, `AdminDatabaseModals` e `AdminDatabaseSections` passaram no ESLint direcionado; falta smoke visual | Smoke clique em categorias, subtabs e deep link com hash |
| Admin taxonomias/filtros | `/admin/operation/database/filters` | CRUD de banca, orgao, foco, cargo, materia, topico e assunto com dependencia pai/filho | OK local parcial | Workflow local e helper de referencias do add/edit questao passam eslint sem avisos; `filters/index.ts` agora normaliza taxonomias com ids string/tipos crus explicitos, `FiltersManagementSection.tsx` foi tipado para a tabela unificada e ficou sem `any` no ESLint direcionado; cargo/topico/assunto continuam exigindo pai | Smoke criar/editar/excluir cada tipo e validar seletores no add/edit questao |
| Comentarios | `comments` + `/admin/support/comments` | Criar/aprovar, moderar quando necessario, notificar admin e autor | OK local parcial | Comentarios voltaram a nascer aprovados por padrao; frontend nao mostra toast de sucesso redundante; editor rico saneia entrada/saida e bloqueia colagem HTML; envio para admins/all nao quebra mais FK em notificacoes; `AdminCommentsModerationSection.tsx` migrou para `React Query` (chave estavel + cache curto + invalidacao por mutacao), eliminando rajadas por loader manual. Baseline dedicado de hard refresh em `/admin/support/comments` mostrou `comments_moderation` 1x, sem duplicatas e sem falhas | Teste de moderacao ponta a ponta em staging |
| Denuncias | `reports` + `/admin/support/reports` | Registrar denuncia, deduplicar, notificar admin e avisar denunciante quando resolvida | Parcial | Notificacao admin corrigida; resolucao admin notifica o denunciante; wiring passa | Testar decisao admin, email e notificacao no painel real |
| Feedback/suporte | `feedback` + `/admin/support/feedback` | Conversa usuario/admin, badge correto, notificacoes e deep link para thread | Parcial | Notificacao admin corrigida; resposta admin notifica o usuario; clique no menu principal Suporte abre a primeira fila com pendencia; `/support?threadId=...` abre a conversa correta; `AdminFeedback.tsx` e `AdminSupportSection.tsx` passam ESLint sem avisos, e a troca de subsecao nao dispara mais `setState` sincrono no efeito; precisa smoke visual com dados reais | Smoke de nova conversa, resposta, notificacao do usuario e clique no badge |
| Depoimentos da home | `api/feedback/testimonials.php`, `/profile`, `/` | Usuario envia avaliacao com nome publico/contexto; admin aprova antes de aparecer na home; sem aprovadas usa mocks | OK local parcial | Validator exige rating, nome publico, contexto e texto; admin publica ao marcar `resolved`; endpoint publico lista apenas completos com `home_published_at`; testes frontend/backend passam | Smoke enviar avaliacao, aprovar em suporte, ver home e reverter status |
| Materiais marketplace | `materials` | Upload, moderacao, compra, bloqueio, notificacao e recompensa | OK local parcial | Notificacao admin para material pendente e notificacao de autor aprovar/rejeitar/remover cobertas; aprovacao gera XP/reputacao/badge idempotente; admin de materiais passa ESLint sem avisos; falta smoke de arquivo protegido | Testar upload real e arquivo protegido |
| Marketplace compra | `payments`, `transactions`, `/marketplace` | Compra material, acesso protegido, notificacao, XP/reputacao e reembolso | OK local parcial | Compra direta preserva `material_id` textual; Stripe verify-payment e compra gratuita notificam comprador/vendedor/admin e geram XP/reputacao/badges por ledger idempotente; provider frontend sem avisos criticos de efeito/deps no recorte; `transactionsService.ts` e `marketplaceService.ts` passaram no ESLint direcionado e a pagina marketplace segue sem avisos, sem mutar `currentUser` apos checkout e sem imagens diretas; falta sandbox real | Teste gateway, notificacoes, XP/reputacao e download |
| Dashboard de parceiro | `/partner-dashboard` | Parceiro acompanha vendas, materiais, notificacoes, dados bancarios e avaliacoes sem render instavel | OK local parcial | Pagina passa ESLint sem avisos e typecheck; saldo/grafico nao usam relogio impuro no render, notificacoes sairam do corpo do componente, previews usam `next/image` e salvar dados bancarios encerra loading corretamente | Smoke parceiro: publicar material, editar capa, responder avaliacao, salvar banco e abrir notificacoes |
| Assinaturas | `subscriptions/*` | Checkout, renovacao, cancelamento, past_due e webhook configuravel sem ambiguidade | OK local parcial | Suite E2E Stripe em modo teste passou (`7 OK`) e o checklist de renovacao passou (`22 OK`). Coberto: renovacao por Test Clock, auto-renew off/on, upgrade com credito proporcional local, refund concorrente, webhook duplicado/fora de ordem/atrasado e reconciliacao. Chaves invalidas continuam barradas; `past_due` notifica aluno/admin; webhook canonico e alias delegam para a mesma rota. Cron e webhook foram travados por teste para nao dependerem de usuario logado/admin. Falta homologar webhook publico/tunel e chaves finais na VPS | Smoke Stripe em staging/VPS com dominio publico |
| Configuracao Stripe | `config/stripe.php`, admin settings, preflight | Aceitar apenas chaves reais e coerentes antes de vender | OK local | Runtime valida `sk_*`, admin valida `pk_*`/`sk_*`/`whsec_*` e preflight reprova formatos invalidos ou modos test/live misturados; UI de metodos Stripe locais nao usa ID temporal em render e passa ESLint | Smoke de checkout/webhook com credenciais reais |
| Config publico de pagamentos | `api/payments/config.php` | Expor somente chave publica Stripe valida e nunca sinalizar checkout ativo com configuracao incompleta | OK local | `publishableKey` invalida vira string vazia e `stripeConfigured` exige publishable valida + secret valida | Smoke frontend checkout/marketplace com Stripe sandbox |
| Financeiro admin | `/admin/financeiro/*` | Ver transacoes, planos, cupons, automacao, reembolsos, analytics e evidencias Stripe | OK local parcial | `AdminFinance.tsx` e `AdminFinanceAnalyticsPanel.tsx` passaram no ESLint direcionado; cupons removidos nao voltam por default e respostas parciais de save nao apagam/ressuscitam cupons; upgrade manual usa provider `manual_admin`, cancela Stripe remoto antes da concessao gratuita e nao deve gerar renovacao; execucao manual de automacao voltou a compilar com resposta tipada, tabelas/CSV/evidencias Stripe ficaram tipadas e build/typecheck passaram. Ainda precisa smoke com gateway real | Smoke transacao, refund, webhook, analytics, automacao e evidencia Stripe |
| Verificacao tardia de pagamento | `api/payments/verify-payment.php` | Confirmar PaymentIntent apenas para usuario autenticado dono da compra | OK local | Rota exige auth e service compara usuario autenticado com metadata/transacao antes de persistir acesso | Smoke compra material em staging |
| Stripe Connect vendedor | `api/payments/create-connect-account.php` | Criar onboarding apenas para usuario autenticado e com redirects seguros | OK local | Payload nao pode trocar `userId`; `returnUrl`/`refreshUrl` aceitam apenas origem da plataforma e HTTPS em producao | Smoke onboarding vendedor em staging |
| Cartao salvo | `users/cards` + Stripe | Nao duplicar cartao, atualizar PM recente | OK local | Teste `UsersCardsStripeMirrorUpsertTest.php` passou apos correcao | Provar com Stripe real |
| Reembolso | `transactions/refund` | Solicitar, aprovar, reconciliar gateway, notificar afetados e ajustar reputacao | OK local parcial | Pedido notifica admins e vendedor; conclusao notifica usuario, vendedor e admins; reembolso de material reduz reputacao do vendedor de forma idempotente; falta gateway real | Testar refund sandbox |
| Cron financeiro | `subscriptions/cron_stripe_reconciliation.php` e `subscriptions/automation_helper.php?action=run_now` | Rodar com `CRON_SECRET`, sem concorrencia excessiva e sem gravar artefato publico | OK local parcial | Secret e lock validados antes do MySQL no cron HTTP; execucao manual admin `run_now` tambem reutiliza o lock `subscriptions_stripe_reconciliation`; log interno vai para `storage/logs/subscriptions`; falta prova carga/agenda | Configurar cron unico, rotacao de log e smoke VPS |
| Cron legado MP | `scripts/tasks/sync_mercadopago_preapproval_plans.php` | Nao quebrar por arquivo ausente e orientar remocao | OK local | Script guard retorna provider removido | Remover cron MP da VPS |
| Notificacoes | `notifications/*`, `/notifications` | Listar apenas ativas, marcar, limpar, restaurar lixeira, enviar e alertar admins/usuarios em eventos criticos | OK local parcial | Schema/listagem corrigidos; feedback respondido, comentario moderado, curtida social e denuncia resolvida notificam usuarios; compra/reembolso de material, `past_due` Stripe e reembolso pendente notificam afetados; pseudo-destinatarios `admin`/`all` agora viram IDs reais antes do insert, removendo a falha de FK; wiring passa; pagina `/notifications` passa ESLint sem avisos, sem relogio impuro no render e com evidencia em `next/image` | Testar UI, unread, restaurar/excluir, deep links e webhooks reais |
| Gamificacao | `questions/answer`, comentarios, denuncias, `rankings/*`, marketplace | XP, level, badges, streak, reputacao, ranking e notificacoes por evento | Parcial | XP/level/streak/badges basicos existem; marketplace, social e rankings possuem `user_gamification_events` para aprovacao/venda/reembolso, comentarios, curtidas, denuncias aceitas, participacao e resultado oficial; ainda falta motor geral/outbox | Expandir motor geral e testar webhooks/rankings reais |
| Rankings | `/ranking`, `rankings/*` | Listar, criar com auth, enviar gabarito sem confiar no `userId` cliente, moderar, notificar e recompensar eventos principais | Parcial | Create/join exigem usuario autenticado; join usa usuario da sessao; ranking pendente notifica admin; moderacao notifica e recompensa criador; participacao/gabarito oficial notificam e recompensam participantes; `rankingsService.ts`, `RankingEditorModal.tsx` e `useRankingEditorWorkflow.ts` passaram no ESLint direcionado, e o editor admin foi alinhado ao modelo real de vagas (`vacanciesAc`, `vacanciesAfro`, `vacanciesPcd`) sem `any`; sem teste E2E | Smoke criar ranking com aluno, aprovar com admin, enviar gabarito, publicar gabarito oficial e abrir notificacoes/badges |
| Simulados | `/simulation`, `simulations/*` | Criar, executar, tempo so em andamento | OK local parcial | Wiring passa; `StudyTrackerProvider` nao usa tempo impuro no render, `/simulation` passa ESLint sem avisos, `simulationsService.ts` foi tipado para persistencia/listagem e o cronometro finaliza pelo tick sem cascata de estado no efeito; falta smoke real do timer e persistencia | Smoke manual iniciado/finalizado, restaurar historico e validar contagem de tempo |
| Cronograma | `/cronograma`, `api/study-schedule/*` | Modulo elite ativavel, plano de estudos persistido por usuario e sem indexacao publica | OK local | Backend persiste por usuario, bloqueia no servidor para Elite/admin, UI migra localStorage antigo e layout usa `noindex`; falta smoke com usuario Elite real | Testar criar/salvar/recarregar/apagar em staging |
| Raio-X | `/x-ray`, statistics | Insights de banca, graficos estaveis | Parcial | Graficos criticos do Raio-X/Bank Analysis usam wrapper que so monta Recharts com dimensao real; falta smoke visual em staging | Smoke responsivo |
| Graficos operacionais | Dashboard, perfil, parceiro e raio-x de banca | Nao emitir warning `width(-1)`/`height(-1)` quando cards/tabs ainda nao mediram | OK local | `ResponsiveContainer` direto removido dos graficos criticos e teste SSR do wrapper passou | Smoke browser desktop/mobile com console limpo |
| Lei comentada | `/lei-comentada` | Leis, comentarios, highlights, admin acervo | Parcial | Build passa; admin save/import exigem smoke funcional e o reader ainda precisa validacao ponta a ponta com conteudo real | Testar artigo, comentario e admin acervo com usuario sem permissao |
| Leitor PDF/material | `PdfViewer` | Abrir PDF, comentarios, anotacoes e marcadores sem render instavel | OK local parcial | `PdfViewer.tsx` passa ESLint sem avisos e typecheck; comentarios foram tipados, `readerService.ts` ficou sem `any` explicito no recorte auditado e efeitos de abrir/recarregar sidebar nao chamam estado sincrono | Smoke abrir PDF, comentar, responder, curtir, anotar e criar marcador |
| Analytics tracking | `api/analytics/track.php` | Registrar eventos sem confiar em identidade enviada pelo cliente e sem aceitar payload abusivo | OK local | Endpoint usa `analytics_track`, usuario autenticado vem da sessao, anonimo nao pode forjar `userId` e `metadata`/`externalHooks` tem limite | Observar volume real, taxa de 429 e eventos de campanha em staging |
| Performance materias | `/performance/subjects` | Insight elite e comparativo da comunidade | Parcial | Build passa; regra elite precisa smoke | Teste usuario gratis/elite |
| Perfil | `/profile` | Dados, cobranca, salvos, avaliar plataforma | OK local parcial | Modal de avaliacao exige nome publico/contexto/depoimento, envia foto quando houver e evita repetir dados administrativos no texto; remocao de cartao/foto e cancelamento de reembolso usam modal oficial em vez de dialogos nativos; `cardsService.ts`, `profileService.ts` e `ProfilePage.tsx` agora passam no ESLint direcionado com contratos tipados para cartoes salvos, transacoes, materiais, referral, foto e notas. Ainda falta smoke completo das abas e fluxos reais com usuario/admin | Testar abas, envio, validacoes e aprovacao admin |
| Campanhas | `/admin/marketing/campaigns`, `scripts/tasks/process_marketing_automations.php` | Notificacao manual, banners, segmentacao, automacoes por condicao e execucao idempotente | OK local parcial | UI salva regras/banners; `AdminMarketing.tsx` passa eslint sem avisos; banners customizados renderizam HTML saneado sem executar scripts arbitrarios do banco; executor CLI processa regras ativas com dry-run, lock, `marketing_automation_events`, notificacao/e-mail, condicoes oficiais e CTAs absolutos apenas em HTTPS; falta smoke com campanha ativa, SMTP real e usuarios elegiveis | Smoke campanha ativa: dry-run, envio real controlado, duplicidade, links e logs |
| Ofertas/countdown | Home/checkout/planos | Mostrar oferta limitada sem instabilidade de hidratacao | OK local parcial | Hook de countdown nao usa `Date.now()` no render e passa eslint sem avisos | Smoke visual de oferta ativa/expirada em desktop/mobile |
| Promocao publica | `/promo/[slug]` | Exibir somente a campanha ativa correta e proteger slugs invalidos de indexacao | OK local | Slug da rota precisa bater com `activePromotion.slug`; metadata SSR indexa somente slug ativo e usa `noindex` para inativo/invalido/modulo desligado | Smoke campanha real no dominio final |
| URLs de campanhas | Admin marketing, notificacoes e banners | CTAs devem ir para rotas seguras e existentes | OK local | Links configuraveis bloqueiam `javascript:`, `data:`, protocolo relativo e caminhos `/admin`/`/api`; fallback padrao agora e `/planos`, nao `/pricing` | Smoke admin criando banner/notificacao |
| Landing pages | `/admin/marketing/landing-pages`, `/l/[slug]` | Lista WP, add/edit, publicar HTML seguro e metadata publica correta | OK local parcial | Editor atual usa estrutura textual; textos/SEO removem markup e canonical inseguro; canonical bloqueia protocolo relativo e caminhos sensiveis; `/l/[slug]` tem metadata server-side para publicadas, `noindex` para rascunhos/inexistentes e sitemap inclui landings customizadas publicadas. HTML bruto futuro deve nascer como feature separada com sandbox/review server-side | Testar publicacao, OG/canonical/sitemap no dominio |
| Logs | `/admin/settings/logs` | Categorias, repeticoes, limpar/baixar e logs operacionais fora da pasta publica | Parcial | Endpoint retorna `analysis`; scanner CLI pronto; cron de assinaturas grava em `storage/logs/subscriptions`. Auth logger agora deduplica eventos ruidosos (`access_token_rejected`/`refresh_reuse_detected`) em janela curta para reduzir spam operacional. Ainda falta calibrar retencao e politica final de verbosidade na VPS | Configurar cron/logrotate, calibrar env de auth logs e testar UI |
| Cache admin | `/admin/settings/cache` | Ver estatisticas, limpar cache, limpar expirados e ajustar TTL sem quebrar runtime | OK local parcial | Tela de cache passa ESLint sem avisos e typecheck; payload tipado e carregamento inicial agendado; falta executar contra cache real na VPS | Smoke estatisticas, clear, clean expired e TTL |
| Auditoria de logs | `scripts/tasks/production_log_audit.php` | Detectar criticos e repeticoes antes de incidente | OK local | Wiring passou; execucao local inspecionou settings/cron sem criticos | Rodar em producao com `LOG_AUDIT_FAIL_ON=critical` |
| Smoke operacional | `scripts/tasks/production_smoke.php` | Validar endpoints publicos, settings sem vazamento e conexao MySQL apos deploy/rollback | OK local | Smoke local passou com API (`plans`, `questions_list`, `settings`), frontend (`/`, `/auth`, `/practice`, `/questions`, `/question/[id]`) e 3 conexoes MySQL; falta execucao na VPS | Rodar na VPS com dominio real |
| Hydration/transicoes | `PageTransition`, `NextRouteFrame` | Primeiro HTML da rota deve bater com o cliente e nao disparar hydration warning | OK local parcial | `PageTransition` foi reduzido a um wrapper SSR estavel, o `ThemeProvider` deixou de calcular tema a partir de `localStorage/matchMedia` na primeira renderizacao e `ThemeOrnaments` ficou deterministico, reduzindo a chance de mismatch e reload apos a pagina pronta. Os testes SSR seguem verdes; falta smoke final em browser com a pagina `/auth` aberta apos restart limpo do dev server para confirmar console limpo | Validar build real no browser desktop/mobile |
| Superficie publica da API/backend | `api/`, raiz backend, `.htaccess`, `production_preflight.php` | Nao publicar logs, dumps SQL, backups, storage ou scripts operacionais | OK local | `subscription_cron.log` removido de `api/`; `backend.zip` movido para fora de `htdocs`; `.htaccess` bloqueia backups/dumps/compactados, diretorios internos e `scripts/` com allowlist apenas do crawler Gran; preflight bloqueia novos artefatos publicos em `api/` e na raiz do backend | Rodar preflight na VPS antes do go live |
| Admin dashboard/painel | `/admin/panel` | Contadores, alertas e saude de billing sem render instavel | OK local parcial | Abas sincronizam no frame seguinte, alertas/billing estao tipados sem `any`, `AdminDashboard.tsx` passou no ESLint direcionado com datasets/transacoes/relatorios tipados e o build continua verde; historico ainda precisa snapshot backend para contadores | Teste de agregacao por dataset e smoke visual |
| Admin usuarios | `/admin/operation/users`, `/admin/operation/users/[userId]/edit` | Listar, abrir perfil, editar e remover usuario no padrao WordPress sem quebrar contratos do perfil | OK local parcial | `AdminUsersSection.tsx`, `UserProfileAdminModal.tsx`, `AdminUserEditorPage.tsx`, `useAdminUserProfileWorkflow.ts` e a pagina standalone `/admin/operation/users/[userId]/edit` passaram no ESLint direcionado; perfil/editor deixaram de sincronizar plano e reset de detalhes via efeito sincrono, previews usam `next/image`, papel legado `tester` foi normalizado no editor e a borda de tipagem do detalhe de usuario foi saneada sem quebrar o build. Falta smoke do modal/perfil/acao de lixeira com dados reais | Smoke abrir perfil, editar usuario e remover |
| Admin marketing navegacao | `/admin/marketing/*` | Alternar landing pages, campanhas, temas e redes sociais sem estado derivado instavel | OK local parcial | `AdminMarketingSection` e `AdminSocialLinksManager` passam ESLint sem avisos; rascunho de redes sociais sincroniza sem `setState` sincrono no efeito | Smoke editar redes sociais, trocar abas e salvar |
| Reset administrativo de banco | `api/admin/reset_db.php` | Nunca ficar executavel por acidente em producao publica | OK local | `APP_ENV=production` forca modo producao, reset fica bloqueado por padrao e preflight falha se `ADMIN_DATABASE_RESET_ENABLED=true` | Manter desativado em producao e usar apenas em janela operacional com backup |
| Modulos ativaveis | `/admin/settings/modules` | Feature flags controlam acesso | Parcial | Build passa; precisa matriz por rota | Teste cada flag |
| SEO | `sitemap.xml`, `robots.txt`, public pages | Indexar publico, noindex privado e canonicals corretos | OK local parcial | `NEXT_PUBLIC_CANONICAL_URL` alimenta metadata/sitemap/robots; cronograma Elite corrigido para `noindex`; sitemap dinamico remove questoes draft/elite/internal, materiais pendentes/rejeitados, rankings pendentes/rejeitados e landings com slug reservado/prefixo sensivel; falta deploy/Search Console | Validar no dominio |
| Seguranca | Auth/CORS/uploads/admin | Cookies, refresh, CSRF, permissoes, XSS e configuracao segura de deploy | Risco | Auth tests passam; XSS em questoes/comentarios/landings mitigado; uploads, rate limit, reset DB guard, preflight de CORS/APP_ENV e bloqueio ampliado de artefatos operacionais em `api/`, raiz do backend, `storage` e `scripts` prontos localmente; HTML bruto futuro deve ser tratado como recurso novo com sandbox/review server-side | Auditoria de endpoints mutaveis e staging |
| Headers de seguranca | Next/API | CSP, clickjacking, nosniff, referrer, permissions policy e deploy em modo producao | OK local parcial | Headers aplicados no Next e na API; CSP do frontend preserva Google/reCAPTCHA/Stripe e remove `unsafe-eval` em producao; smoke local confirmou API; preflight barra `APP_ENV` fora de production, `APP_URL` local e CORS sem HTTPS/wildcard | Validar no proxy HTTPS da VPS |
| Uploads | Materiais, perfil, contexto de questoes | Validar MIME real, tamanho, extensao segura e impedir execucao em `/uploads` | OK local | Helper central aplicado e `.htaccess` endurecido; wiring passou | Validar regra equivalente no servidor final |
| Rate limiting | Auth, comentarios, feedback, denuncias, uploads e analytics | Bloquear abuso por IP e assunto sensivel, retornar 429 com `Retry-After` | OK local | Perfis aplicados; proxy headers ignorados por padrao; preflight valida runtime gravavel; analytics tem perfil proprio 120/min | Calibrar limites em staging e monitorar falsos positivos |
| Banco | MySQL | Env, indices, integridade, schema, backup | OK local parcial | DB env corrigido; script de backup/verificador prontos; schema do marketplace normalizado para `user_id`, `material_id` textual, ratings/notas compativeis e ledger `user_gamification_events`; falta restore real e carga | Teste dump/restore, carga, indices e migration em staging |
| Backup operacional | `scripts/tasks/backup_mysql.php`, `verify_mysql_backup.php`, `restore_mysql_backup.php` | Dump diario com checksum, retencao, restore seguro e smoke pos-restore | Parcial | Local pronto: backup/verificador/restore dry-run, bloqueio do banco atual e bloqueio de dump destrutivo. Falta restore real em banco temporario da VPS | Rodar backup real, restore em staging/VPS e smoke apontado ao banco restaurado |

